package org.jboss.logging.tools.provisioning.descriptor;

/** Maps to @Message.Format — how the message string is formatted at runtime. */
public enum MessageFormat {
    PRINTF,
    MESSAGE_FORMAT,
    NO_FORMAT
}
